//Ask the user to enter a number, execute the for loop from 1 to that number, 
// and find the sum of all numbers in that range

let usernum = Number(prompt("Enter a Number:"))

for(let sum = 0; sum < usernum; sum ++){
    console.log(sum)
}

